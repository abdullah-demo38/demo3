import { Check } from 'lucide-react';
import { useState } from 'react';
import { Reveal, SectionHeading } from './primitives';

const plans = [{ name: 'Foundation', description: 'For businesses building a stronger digital foundation.', monthly: '299', yearly: '239', features: ['Website health review','Local presence essentials','Visibility recommendations','Monthly performance snapshot'] }, { name: 'Growth', description: 'For businesses focused on visibility and lead generation.', monthly: '599', yearly: '479', recommended: true, features: ['Everything in Foundation','AI search visibility insights','Content opportunity planning','Lead and inquiry tracking','Reputation support'] }, { name: 'Advanced', description: 'For businesses looking for a complete AI-powered growth system.', monthly: 'Custom', yearly: 'Custom', features: ['Everything in Growth','Multi-location visibility','Advanced content workflows','Priority strategy support','Custom reporting'] }];

export function Pricing() { const [yearly, setYearly] = useState(false); return <section id="pricing" className="relative z-10 py-28 sm:py-36"><div className="mx-auto max-w-7xl px-6"><SectionHeading eyebrow="Plans" title={<>A clearer path to <span className="gradient-text">better visibility.</span></>} subtitle="Choose the level of support that fits your next stage. Every plan starts with a practical view of your current digital presence." /><div className="mt-8 flex justify-center"><div className="flex rounded-xl border border-white/10 bg-white/[0.03] p-1 text-sm"><button onClick={() => setYearly(false)} className={`rounded-lg px-4 py-2 ${!yearly ? 'bg-white/10 text-white' : 'text-white/45'}`}>Monthly</button><button onClick={() => setYearly(true)} className={`rounded-lg px-4 py-2 ${yearly ? 'bg-white/10 text-white' : 'text-white/45'}`}>Yearly <span className="ml-1 text-cyan">Save 20%</span></button></div></div><div className="mt-12 grid gap-5 lg:grid-cols-3">{plans.map((plan, index) => <Reveal key={plan.name} delay={index * 0.08}><div className={`relative flex h-full flex-col rounded-3xl p-7 ${plan.recommended ? 'border border-cyan/50 bg-cyan/[0.06] shadow-[0_0_50px_rgba(34,211,238,0.08)]' : 'border border-white/10 glass'}`}>{plan.recommended && <span className="absolute right-6 top-6 rounded-full bg-cyan px-3 py-1 text-[10px] font-bold uppercase tracking-widest text-ink">Recommended</span>}<h3 className="font-display text-xl font-semibold capitalize text-white">{plan.name}</h3><p className="mt-3 min-h-12 text-sm leading-relaxed text-white/50">{plan.description}</p><div className="mt-7 text-3xl font-semibold text-white">{plan.monthly === 'Custom' ? 'Let’s talk' : `$${yearly ? plan.yearly : plan.monthly}`}<span className="text-sm font-normal text-white/35">{plan.monthly === 'Custom' ? '' : ' / month'}</span></div><ul className="mt-7 flex-1 space-y-3 border-t border-white/10 pt-6">{plan.features.map((feature) => <li key={feature} className="flex gap-2 text-sm text-white/65"><Check size={16} className="mt-0.5 shrink-0 text-cyan" />{feature}</li>)}</ul><a href="#contact" className={`mt-8 rounded-xl px-4 py-3 text-center text-sm font-semibold transition ${plan.recommended ? 'bg-cyan text-ink hover:bg-white' : 'bg-white/8 text-white hover:bg-white/15'}`}>{plan.name === 'Advanced' ? 'Talk to an expert' : 'Get started'}</a></div></Reveal>)}</div></div></section>; }
