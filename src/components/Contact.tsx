import { useState, type FormEvent } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { Send, CheckCircle2, Loader2, AlertCircle } from 'lucide-react';
import { Reveal, SectionHeading } from './primitives';

const businessTypes = ['Hotel or hospitality', 'Restaurant or food service', 'Healthcare or dental', 'Real estate', 'Home services', 'Professional services', 'Other'];
type Status = 'idle' | 'loading' | 'success' | 'error';

export function Contact() {
  const [status, setStatus] = useState<Status>('idle');
  const [error, setError] = useState('');
  const onSubmit = async (event: FormEvent<HTMLFormElement>) => {
    event.preventDefault(); setStatus('loading'); setError('');
    const form = event.currentTarget; const values = Object.fromEntries(new FormData(form).entries());
    try {
      const response = await fetch(`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/contact-notification`, { method: 'POST', headers: { 'Content-Type': 'application/json', apikey: import.meta.env.VITE_SUPABASE_ANON_KEY }, body: JSON.stringify({ name: values.name, business: values.business, email: values.email, phone: values.phone, businessType: values.businessType, message: values.message }) });
      const result = await response.json() as { error?: string };
      if (!response.ok) throw new Error(result.error ?? 'We could not send your request.');
      setStatus('success'); form.reset();
    } catch (caught) { setError(caught instanceof Error ? caught.message : 'We could not send your request. Please try again.'); setStatus('error'); }
  };
  return <section id="contact" className="relative z-10 py-28 sm:py-36"><div className="mx-auto max-w-7xl px-6"><div className="grid gap-12 lg:grid-cols-2"><div><SectionHeading center={false} eyebrow="Start a conversation" title={<>Let's talk about <span className="gradient-text">your business.</span></>} subtitle="Tell us where your digital presence needs to go. We’ll help you find the clearest next step." /><Reveal delay={0.2}><div className="mt-8 space-y-4">{[['Email','hello@aiomatrix.com'],['Response time','Within one business day'],['First step','A practical visibility conversation']].map(([label, value]) => <div key={label} className="flex items-center gap-4 rounded-2xl glass px-5 py-4"><span className="text-xs uppercase tracking-widest text-white/40">{label}</span><span className="text-sm font-medium text-white">{value}</span></div>)}</div></Reveal></div><Reveal delay={0.15}><div className="rounded-3xl glass-strong p-6 sm:p-8"><AnimatePresence mode="wait">{status === 'success' ? <motion.div key="success" initial={{ opacity: 0, scale: .95 }} animate={{ opacity: 1, scale: 1 }} className="flex min-h-[460px] flex-col items-center justify-center text-center"><div className="grid h-16 w-16 place-items-center rounded-2xl bg-cyan text-ink"><CheckCircle2 size={32} /></div><h3 className="mt-6 font-display text-2xl font-semibold text-white">Thank you. Your message has been received.</h3><p className="mt-2 max-w-sm text-sm text-white/55">We'll get back to you shortly.</p><button onClick={() => setStatus('idle')} className="mt-6 rounded-xl glass px-5 py-2.5 text-sm font-medium text-white/80 hover:text-white">Send another request</button></motion.div> : <motion.form key="form" onSubmit={onSubmit} className="space-y-5"><div className="grid gap-5 sm:grid-cols-2"><Field label="Full Name" name="name" placeholder="Sarah Mitchell" /><Field label="Email" name="email" type="email" placeholder="you@business.com" /></div><div className="grid gap-5 sm:grid-cols-2"><Field label="Business Name" name="business" placeholder="Your business" /><Field label="Phone" name="phone" type="tel" placeholder="(555) 000-0000" /></div><div><label className="mb-2 block text-xs uppercase tracking-widest text-white/40" htmlFor="businessType">Business Type</label><select id="businessType" name="businessType" required defaultValue="" className="w-full rounded-2xl glass px-4 py-3 text-sm text-white outline-none"><option value="" disabled>Select a category</option>{businessTypes.map((type) => <option key={type} value={type} className="bg-ink">{type}</option>)}</select></div><div><label className="mb-2 block text-xs uppercase tracking-widest text-white/40" htmlFor="message">Message</label><textarea id="message" name="message" rows={4} required minLength={10} placeholder="What would you like to improve?" className="w-full resize-none rounded-2xl glass px-4 py-3 text-sm text-white placeholder-white/30 outline-none transition focus:ring-1 focus:ring-electric/30" /></div>{status === 'error' && <div role="alert" className="flex gap-2 rounded-xl border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200"><AlertCircle size={17} className="shrink-0" />{error}</div>}<button type="submit" disabled={status === 'loading'} className="group relative inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-gradient-to-r from-electric via-purple to-cyan px-7 py-3.5 text-sm font-semibold text-white shadow-glow transition-colors disabled:opacity-60">{status === 'loading' ? <><Loader2 size={16} className="animate-spin" />Sending...</> : <>Send message <Send size={16} /></>}</button></motion.form>}</AnimatePresence></div></Reveal></div></div></section>;
}
function Field({ label, name, type = 'text', placeholder }: { label: string; name: string; type?: string; placeholder?: string }) { return <div><label className="mb-2 block text-xs uppercase tracking-widest text-white/40" htmlFor={name}>{label}</label><input id={name} type={type} name={name} required={name !== 'phone'} placeholder={placeholder} className="w-full rounded-2xl glass px-4 py-3 text-sm text-white placeholder-white/30 outline-none transition focus:ring-1 focus:ring-electric/30" /></div>; }
