import { motion } from 'framer-motion';
import { BarChart3, Link2, Search, Sparkles } from 'lucide-react';
import { Reveal, SectionHeading } from './primitives';
const steps = [[Link2,'Connect','Connect your website and business information.'],[Search,'Analyze','Identify visibility, content, and conversion opportunities.'],[Sparkles,'Improve','Use intelligent recommendations and tools to strengthen your digital presence.'],[BarChart3,'Grow','Monitor progress, capture leads, and keep improving.']];
export function Process() { return <section id="process" className="relative z-10 py-28 sm:py-36"><div className="mx-auto max-w-7xl px-6"><SectionHeading eyebrow="How it works" title={<>From setup to growth, <span className="gradient-text">without the complexity.</span></>} /><div className="mt-16 grid gap-5 md:grid-cols-4">{steps.map(([Icon, title, description], index) => { const StepIcon = Icon as typeof Link2; return <Reveal key={title as string} delay={index * .08}><motion.div whileHover={{ y: -8 }} className="relative h-full rounded-2xl glass p-6"><div className="mb-8 flex items-center justify-between"><div className="grid h-12 w-12 place-items-center rounded-xl bg-cyan/10 text-cyan"><StepIcon size={22} /></div><span className="font-mono text-xs text-white/30">0{index + 1}</span></div><h3 className="font-display text-lg font-semibold uppercase text-white">{title as string}</h3><p className="mt-2 text-sm leading-relaxed text-white/50">{description as string}</p></motion.div></Reveal>; })}</div></div></section>; }
